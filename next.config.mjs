/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "utfs.io",
      "assets.aceternity.com",
      "subdomain",
      "images.unsplash.com",
    ],
  },
  reactStrictMode: false,
};

export default nextConfig;
